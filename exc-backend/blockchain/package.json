{
  "name": "blockchain",
  "version": "1.0.0",
  "description": "Blockchain module for exchange backend",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "clean": "npx hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "hardhat": "^2.28.6"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "@chainlink/contracts": "^1.5.0"
  }
}
